-- ============================================================
-- SEED: Test data cho OTP (chỉ dùng trong môi trường dev)
-- Không có OTP nào được seed — OTP sinh động qua API
-- File này chỉ để đảm bảo bảng tồn tại và index hoạt động đúng
-- ============================================================
-- Không INSERT gì cả — OTP là dữ liệu động, không seed cứng
SELECT 'OTP tables ready' AS status;
