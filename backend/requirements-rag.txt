# ─────────────────────────────────────────────────────────────
# RAG stack đầy đủ
# Cài sau requirements.txt nếu môi trường hỗ trợ
# pip install -r requirements.txt
# pip install -r requirements-rag.txt
# ─────────────────────────────────────────────────────────────
numpy>=1.26.0
sentence-transformers>=3.0.0
qdrant-client>=1.9.0
google-genai>=2.0.0

# FAISS (tùy chọn — dùng nếu cần local vector search thay Qdrant)
faiss-cpu>=1.8.0